name: Publish dsnix

# Tag namespace: `mimo-dsnix-v<semver>` triggers npm publish for the
# mimo-dsnix shim package only. Plain `v<semver>` tags publish the main
# `mimo-reasonix` CLI (see publish-npm.yml); the two are decoupled so the
# alias can be released independently when the shim itself changes.
on:
  push:
    tags: ["mimo-dsnix-v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to publish (e.g. mimo-dsnix-v0.1.0). Required when running manually."
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: publish dsnix to npm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Setup Node 22
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          registry-url: https://registry.npmjs.org/

      - name: Verify tag matches packages/dsnix/package.json version
        shell: bash
        run: |
          tag="${{ github.event.inputs.tag || github.ref_name }}"
          tag_version="${tag#mimo-dsnix-v}"
          pkg_version=$(node -p "require('./packages/dsnix/package.json').version")
          if [ "$tag_version" != "$pkg_version" ]; then
            echo "::error::tag $tag (version $tag_version) does not match packages/dsnix/package.json version $pkg_version"
            exit 1
          fi
          echo "tag $tag matches packages/dsnix/package.json $pkg_version"

      - name: Verify mimo-reasonix@<version> is already published on npm
        shell: bash
        run: |
          dep_version=$(node -p "require('./packages/dsnix/package.json').dependencies['mimo-reasonix']")
          if ! npm view "mimo-reasonix@${dep_version}" version >/dev/null 2>&1; then
            echo "::error::dsnix depends on mimo-reasonix@${dep_version} but that version is not published yet"
            exit 1
          fi
          echo "mimo-reasonix@${dep_version} is published — safe to publish dsnix"

      - name: Publish mimo-dsnix to npm
        run: npm publish --access public
        working-directory: packages/dsnix
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
